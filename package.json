{
  "name": "linkhub",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Hub personale di Luca Panteghini - sostituto di Linktree",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-assets.mjs",
    "sync:build": "node scripts/sync-assets.mjs && astro build"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
